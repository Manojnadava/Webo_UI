Create a initial react+vite initial setup application 
Create Git repo to track the project by git init command 
Creta a tailwind clone to projcet for css component
Also use daisy UI to get pre built coponernts


?? Now create Componet files like Navbar

// Then we need to set up Routing